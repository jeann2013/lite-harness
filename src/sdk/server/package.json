{
  "name": "@lite-harness/sdk-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Stdio stream-json backend server: native provider agent SDKs behind one contract.",
  "bin": {
    "lite-harness-server": "./server.mjs"
  },
  "scripts": {
    "test": "node --test \"../../tests/sdk/server/**/*.test.mjs\""
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0",
    "@openai/agents": "^0.2.0",
    "openai": "^6.0.0"
  }
}
